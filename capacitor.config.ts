import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'popover-arrow-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
